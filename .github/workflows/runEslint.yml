on: [push]
name: run eslint
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          node-version: '16.13.1'
          cache: 'yarn'

      - run: yarn install
      - run: yarn lint
