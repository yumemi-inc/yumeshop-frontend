import { Box } from '@chakra-ui/react';
import React, { VFC } from 'react';

interface PicAndTextCardProps {
  key: string;
  title: string;
  thumbnailUrl: string;
}

export const PicAndTextCard: VFC<PicAndTextCardProps> = () => <Box />;
